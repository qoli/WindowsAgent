# Docked cockpit menu text regions

Return raw resident PP-OCR text boxes from the centered Elite Dangerous docked
cockpit menu. The bounded reference region covers `STARPORT SERVICES`,
`AUTO LAUNCH`, and `DISEMBARK`. This finite primitive never interprets docking
state or injects input; its owning workflow must require current text evidence.
