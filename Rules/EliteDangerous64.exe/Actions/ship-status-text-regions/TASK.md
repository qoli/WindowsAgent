# Elite Dangerous ship-status text regions

Capture the reviewed lower-right HUD search region at reference density, run the
resident PP-OCRv6 small DirectML detection and w480 recognition models, and
return every text quadrilateral with same-frame pixels immediately to its
left. This Action provides raw OCR evidence only; it does not classify ship
state or silently switch models or providers.
