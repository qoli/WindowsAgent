# Exit destination menu text regions

Capture only the two reviewed destination cards shown after selecting `EXIT`
from the in-game pause menu. The raw OCR regions and their left-context pixels
are consumed by `pause-at-exit-for-human-takeover`; this Action does not choose
an exit destination or inject input.

