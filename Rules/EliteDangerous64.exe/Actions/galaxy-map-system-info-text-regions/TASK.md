# Galaxy Map selected System information text regions

Return raw PP-OCR text boxes from the fixed right-side Galaxy Map System
information panel. A caller may confirm that the selected System's normalized
name exactly matches its requested destination. It does not infer selection
from the search field, accept a similar name, send input, or prove a route.
