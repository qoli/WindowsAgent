# Leased diagonal ship attitude hold

Resolve one pitch and one yaw control from the active Frontier preset, press
both keys into an overlapping hold under one lease ID, and release both on
explicit STOP, lease expiry, partial START failure, or Agent shutdown.

This finite Action proves only grouped key injection. Compass movement remains
the caller's observation Gate.
