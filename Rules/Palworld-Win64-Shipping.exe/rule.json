{
  "schemaVersion": 4,
  "description": "ScreenParser is a directly callable Action that may be explicitly registered as a Monitor or Reaction; no registration is active by default.",
  "runtimeProfiles": {},
  "actions": {
    "screenparser/ui-elements": {
      "path": "Actions/screenparser",
      "runtime": "screenparser-onnx-dml-v1",
      "registrableAs": ["monitor", "reaction"]
    }
  },
  "registrations": {}
}
