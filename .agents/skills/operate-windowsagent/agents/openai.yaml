interface:
  display_name: "Operate WindowsAgent"
  short_description: "Discover, invoke, and verify live WindowsAgent capabilities"
  default_prompt: "Use $operate-windowsagent to inspect the live foreground Rule, choose the highest-level verified Action, and validate the result from runtime and domain evidence."
