interface:
  display_name: "Use Visual Log"
  short_description: "Locate and verify recorded WindowsAgent evidence"
  default_prompt: "Use $use-visual-log to correlate the Action timeline with the untrusted visual index, then retrieve and verify the authoritative Evidence range."
