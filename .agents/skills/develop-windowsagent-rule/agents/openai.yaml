interface:
  display_name: "Develop WindowsAgent Rule"
  short_description: "Build and validate game-owned Rules and Actions"
  default_prompt: "Use $develop-windowsagent-rule to design, implement, and validate a game-owned Rule or Action without taking ownership of WindowsAgent runtime internals."
