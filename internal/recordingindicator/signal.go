// Package recordingindicator exposes the session-local presence signal used
// by an Evidence recorder and the display-only OSD. The signal carries no
// recording control or evidence data.
package recordingindicator

const SignalName = `Local\WindowsAgent.Evidence.Recording.v1`
